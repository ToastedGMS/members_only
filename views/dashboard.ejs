<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/css/dashboard.css" />
		<title>Dashboard</title>
	</head>
	<body>
		<header>
			<h1>Welcome, <%= name %></h1>

			<div class="header-links">
				<a href="/logout">Log out</a>
				<a href="/">Home</a>
			</div>
		</header>
		<% if (error) { %>
		<p style="color: red">Error: <%= error %></p>
		<% } %> <% if (message) { %>
		<p style="color: green">Message: <%= message %></p>
		<% } %>
		<div class="main">
			<form action="/newMessage" method="POST" class="message-form">
				<h1>Submit a Message</h1>
				<div class="form-group">
					<label for="title">Message Title:</label>
					<input
						type="text"
						name="title"
						id="title"
						placeholder="Subject"
						maxlength="30"
						required
					/>
				</div>
				<div class="form-group">
					<label for="message">Your Message:</label>
					<textarea
						id="message"
						name="message"
						rows="4"
						placeholder="Type your message here... (Messages are limited to 140 characters)"
						maxlength="140"
						required
					></textarea>
				</div>
				<div>
					<button type="submit">Send Message</button>
				</div>
			</form>
			<form action="/dashboard" method="post" class="secret-code-form">
				<div class="form-group">
					<label for="code">Do you have a secret code?</label>
					<input type="text" name="code" id="code" />
				</div>
			</form>
		</div>
	</body>
</html>
